{
	"name": "server",
	"version": "0.0.1",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"dev": "bun --watch run src/index.ts",
		"seed": "bun run src/scripts/seed.ts"
	},
	"dependencies": {
		"hono": "^4.10.8",
		"postgres": "^3.4.7",
		"shared": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
